[pytest]
addopts = -q --cov=stockfind --cov=eval --cov-report=term-missing --cov-report=json:results/coverage.json
markers =
    unit: pure-logic tests with no external dependency
    integration: tests that hit the Postgres database or full API
